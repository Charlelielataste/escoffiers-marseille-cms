import articles from './articles'
import events from './events'
import members from './members'
import settings from './settings'
import tags from './tags'

export const schemaTypes = [articles, events, members, settings, tags]
